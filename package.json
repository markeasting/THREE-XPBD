{
  "name": "three",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test_old": "mocha --require ts-node/register src/**/*.spec.ts --watch-extensions ts --watch --watch-files src 'src/**/**.spec.ts' --reporter dot",
    "test": "jest src --watch",
    "asbuild:debug": "asc src/wasm/src/index.ts --target debug",
    "asbuild:release": "asc src/wasm/src/index.ts --target release",
    "asbuild": "npm run asbuild:debug && npm run asbuild:release",
    "start": "npx serve ."
  },
  "devDependencies": {
    "@types/jest": "^29.2.6",
    "@types/mocha": "^10.0.1",
    "@types/three": "^0.148.0",
    "assemblyscript": "^0.26.4",
    "jest": "^29.3.1",
    "mocha": "^10.2.0",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "vite": "^4.0.4",
    "vite-plugin-assemblyscript-asc": "^1.0.5"
  },
  "dependencies": {
    "@swc/jest": "^0.2.24",
    "three": "^0.148.0"
  },
  "exports": {
    ".": {
      "import": "./src/wasm/build/release.js",
      "types": "./src/wasm/build/release.d.ts"
    }
  }
}
