{
  "name": "three",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "mocha --require ts-node/register src/**/*.spec.ts --watch-extensions ts --watch --watch-files src 'src/**/**.spec.ts' --reporter dot"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/three": "^0.148.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "vite": "^4.0.4"
  },
  "dependencies": {
    "three": "^0.148.0"
  }
}
